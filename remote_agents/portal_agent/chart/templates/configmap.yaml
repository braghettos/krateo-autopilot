apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portal-agent.fullname" . }}
  labels:
  {{- include "portal-agent.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.service.port }}"
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}