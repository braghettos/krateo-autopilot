apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "restaction-agent.fullname" . }}
  labels:
  {{- include "restaction-agent.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.service.port }}"
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}