{{- /* Stamp the deployed tag (autopilot version = release tag) into the raw-doc URL for tokenless fetch. */ -}}
{{ .Files.Get (printf "files/prompts-%s.yaml" .Values.prompts.language) | replace "__DOC_TAG__" .Chart.Version }}
